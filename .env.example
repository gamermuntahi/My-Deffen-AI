# =============================================================================
# Deffen AI — environment template
# -----------------------------------------------------------------------------
# Copy this file to `.env` (which is git-ignored) and paste your OpenRouter key.
#
# `.env` holds ONLY the API key. Every other AI setting (endpoint, model,
# timeout, system prompt, ...) lives in config.yml.
#
#   Local dev : cp .env.example .env   (then edit .env)
#   Vercel    : Project -> Settings -> Environment Variables (Production scope)
#
# NEVER commit a real key.
# =============================================================================

OPENROUTER_API_KEY=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
